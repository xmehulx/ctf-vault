source "https://rubygems.org"

# Use latest stable rake
gem 'rake', '>= 13.0'

# Force the protobuf version compatible with latest sass-embedded
gem 'google-protobuf', '>= 4.31.0'

# Lock sass-embedded to a recent, stable version
gem 'sass-embedded', '>= 1.78.0'

# Add Jekyll and theme gems
gem 'jekyll', '~> 4.3'
gem 'jekyll-text-theme', '~> 2.2'
gem 'jekyll-feed', '~> 0.17'
gem 'jekyll-sass-converter', '~> 3.1'

# Optional: if using github-pages
# gem 'github-pages', group: :jekyll_plugins
